本软件是做一个pc工具箱，界面优美，功能丰富。

1. 检查更新
2. 固定页面大小，所有页面设计和编写均完整显示在固定大小中
3. excel工具：
   1. 对固定格式的excel文件进行处理：
      1. 指定第一行结构，用可选的特定分隔符隔开。
      2. 对第二行开始的每一行，进行特定的数据处理。
         1. 支持按某列数据分组，支持将分组后的数据输出为分表，支持按某列排序（可选升、降序），支持分组后到处json动态绘制图表。
      3. 支持对于多分表的处理：多个分表可以将数据合并为主表后，再进行主表的处理。
4. pdf工具：
   1. 支持pdf转高清图片，支持pdf转扫描件，支持多个pdf合并为一个pdf，支持一个pdf分割为多个pdf，支持一个pdf进行切割（支持指定切割页码【从哪页开始、到哪页结束、指定具体页码】）
5. 公章生成工具
6. 暂未设计