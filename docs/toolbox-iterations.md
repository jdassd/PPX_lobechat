# 工具箱连续迭代记录

## 当前执行约束

- 2026-09-08 用户要求继续逐版本实现与发布，不再因单个阶段完成提前结束。
- 本轮截止时间：北京时间 2026-09-09 01:50（UTC 2026-09-08 17:50）；额度耗尽时停止，不自动使用重置额度。
- 每个版本围绕完整且实用的用户流程，完成验证、提交并推送后，再打 Tag 触发 GitHub 自动发版。
- 保留已有未跟踪的 `.workflow/` 和 `design_handoff_工具箱重构/`。仅提交相关改动。

## 已完成基线

- v2.9.0 已发布：15 个结果接力目标、主输入隔离、兼容说明、离线字体与短窗优化。
- 发布提交：`4664e2f`；后续自动发版标签注释修正：`ace8288`，均已推送。
- v2.9.0 及上述流程修正的三平台检查、构建均通过。

## 已发布：v2.10.0 表格处理与单表接力

- 新增单份 OOXML 工作簿到数据质检、数据清洗、按列拆分的接力入口，总计 18 个目标。
- 规则参数显式传递并区分原始质检、清洗与合并；旧全局注入并不等于所有控件失效。
- 修复样本预览丢失类型与公式元数据、合并清洗不一致、快速换文件的陈旧响应与结果。
- 输出准确的处理前行数、输出行数、去重删除数；验证真实 XLSX 输入和导出。
- 状态：已发布，提交 `20581b21c38f1e85c0c4a78803e1a5dd20fccbda`，推送成功。分支 CI `34229803547`、Tag CI `34231352101` 均通过，三平台安装包已上传并检查摘要。
- [Release](https://github.com/jdassd/PPX_lobechat/releases/tag/v2.10.0)。后续创建附注标签使用 `--cleanup=verbatim`，避免 Git 默认将 Markdown 标题当作注释剔除；v2.10 Release 说明已恢复文档标题，发布标签不改写。

## v2.11.0 接力来源与任务追踪

- 为 18 个接收目标和质检报告显式提交主输入来源，元数据不进入业务参数。
- 后端验证路径关系、持久化、重试过滤、来源清理后保留；来源查看支持筛选外查询、逐层返回和来源结果再次接力。
- 工作区配置不保存来源，JSON/CSV 历史导出保留来源字段；不宣称内容哈希溯源。
- 提交 `11e497712455130a52208b20166a19ec6161bc75` 已推送。77 项 Python、16 项前端规则测试、静态检查与完整真实浏览器集成通过，分支 CI `34233841697` 三平台通过。
- 附注 Tag `v2.11.0` 已推送，Tag CI `34235198861` 三平台与自动发布均通过。2026-09-08 14:28 UTC 发布，三份安装包大小、SHA-256 与发布说明已核对；[Release](https://github.com/jdassd/PPX_lobechat/releases/tag/v2.11.0)。

## v2.12.0 常用工作流与可靠续跑

- 新增图片压缩归档、Excel 清洗质检两个常用模板，沿用已有白名单工作流引擎。
- 当前编辑验证并保存后运行，运行输入提前显示，工作流历史能按步骤检查与继续处理结果。
- 新模板部分成功停止并保留产物，修复失败项后从任务中心续跑；修复空密码禁用重试但保留真实敏感参数限制。
- 85 项 Python、20 项前端规则测试、静态检查、构建及完整浏览器集成通过；定向验证两模板、实际 ZIP/XLSX、来源与部分失败续跑通过，短窗口浅/深色及结果截图已存档。
- 提交 `e3a3c3e6c7fc6fee34d781f88498c9c989ea2f0e` 已推送，分支 CI `34238024220`、Tag CI `34240299319` 三平台及自动发布均通过。2026-09-08 15:02 UTC 发布，三份安装包、SHA-256 和说明已核对（说明仅换行符差异）；[Release](https://github.com/jdassd/PPX_lobechat/releases/tag/v2.12.0)。后续标签说明显式写入 LF。

## v2.13.0 执行前检查与恢复说明

- 已实现有规模预算的只读预检：已知输入类型、缺失变量、前序引用、错误定位、编辑失效与迟到响应保护。
- 立即运行先检查再保存执行，新建也可直接运行；参数列表显示数量，切换有效 JSON 清除旧错误。
- 续跑记录来源与实际已知计数；整步复用明确标记，未知数量不猜测；部分成功产物缺失时也拒绝续跑。
- Python 103 项、前端规则 20 项、完整浏览器工作区验证、生产构建和静态检查均通过（147 条既有样式警告、0 错误）。浅/深色预检与续跑截图和证据已存档。
- 完整流程发现并修正组件库数字输入的陈旧 `aria-disabled`；同时验证多轮恢复沿用完整结果计数，未知数量不当作零。
- 提交 `ceaa184c095cc50ba50ac679ddcd8ff5584f8aa6` 已推送；分支 CI `34243203626` 和 Tag CI `34245535163` 三平台与自动发版均通过。2026-09-08 15:47 UTC 发布，三份安装包、SHA-256 与完整说明已核对；[Release](https://github.com/jdassd/PPX_lobechat/releases/tag/v2.13.0)。

## v2.14.0 结果字段与文本处理链路

- 操作目录声明实际结果字段，按模式与输入类型筛选；文件列表、第一份文件、文本、结构化结果和统计各自引用实际路径。
- 新增第五个文本去重统一格式模板，运行记录按需打开数据结果；零、开关、空字符串和长文本保留语义，预览限制与完整复制分开。
- 修正 JSON 查询路径、操作模式、转换方式和替换规则类型。浏览器验证发现并修正 JSON 编辑保留旧参数的问题；无效编辑即时进入检查，保留光标和输入格式。
- Python 108 项、前端规则 25 项、生产构建、静态检查和完整浏览器工作区回归通过，页面错误为零；已存档短窗口浅色/深色及字段选择截图。剪贴板载荷与失败提示使用浏览器内替身验证，不冒充原生系统权限验收。
- 提交 `a11d440ea405782581ffa7fa5d7f66cde1480baf` 已推送；分支 CI `34247563541`、Tag CI `34249835545` 三平台与自动发布均通过。2026-09-08 16:31 UTC 发布，三份安装包、SHA-256 与发布说明已核对；[Release](https://github.com/jdassd/PPX_lobechat/releases/tag/v2.14.0)。

## v2.15.0 完整搜索与相对路径归档

- 已统一搜索的字面匹配、递归、过滤、链接与内部目录范围，明确扫描预算、读取错误、截断及已返回数量。修正旧数量上限传参错误。
- 新增第六个搜索归档模板，不完整搜索停止归档；模板排除输出目录，归档保留相对路径、检测同名及文件目录冲突、失败不覆盖已有产物。
- 搜索结果可经检查后接力；手动归档可选共同根目录，隐藏的 7Z 密码不传给 ZIP。
- 查询源文件不再误入工作流生成文件名单；旧历史按可确认的身份修正，实际源文件修改仍能触发监听。
- Python 130 项、前端 25 项、静态检查（147 条既有警告、0 错误）、生产构建、完整工作区与最终定向浏览器回归通过；短窗口浅/深色及停止归档截图已检查并存档。
- 提交 `a8b550f172e497e715f96b1398d49adb3ceaee9f` 已推送。CI 暴露 macOS 临时目录别名和 Windows 短路径导致测试比较拼写而非规范身份；两轮修正补齐单项与集合断言，最终提交 `d87542ab651a4ed0eb19166f62d091baea3674e0` 已推送，分支 CI `34253061057` 三平台通过。附注 Tag `v2.15.0` 已推送并触发 Tag CI `34254869922`，待核对安装包。

## 当前版本：v2.16.0 重复文件审查与核对报告

- 已实现按大小筛选的有界内容比较、同名候选语义、硬链接逻辑体积、可取消队列任务、来源变化检查与完整性说明。
- 新增独立 Excel 快照报告与第七个工作流模板；不完整自动扫描停止，手动可导出明确标注的不完整报告；真实报告可接到已有 Excel/索引/归档目标。
- 界面逐组审查，每组每页 20 份；修改条件清除旧结果，旧工作区不恢复过期扫描，报告路径改变清除旧报告入口。
- 补充检查修正“部分成功后继续”场景：上游重新执行后，按绑定关系重新生成报告及归档，不累计旧查询匹配；无关步骤保留，历史说明原因。
- 62 个操作的非空默认参数纳入一致性检查，音频提取质量改用实际支持的枚举值。
- Python 149 项、前端 25 项、静态检查（147 条既有警告、0 错误）、生产构建与完整浏览器工作区回归通过，页面错误为零；2000 份真实文件的扫描与完整报告读取通过。短窗口浅/深色、不完整分页和重新生成报告说明已检查并存档。v2.15 三平台构建已完成，准备提交并推送本版。

## 后续候选方向

1. 操作目录与真实输入输出契约对齐，特别是工作流结果字段选择，减少可提前避免的链路配置错误。
2. 打包后的自检与恢复、稳定性和高频交互优化。
3. 空密码禁用重试的问题已在 v2.12 修复并做真实 ZIP 验证；后续检查以实际错误和用户流程为依据。
4. 预检边界、错误明细截断、迟到响应、缺失部分产物和陈旧 JSON 错误已在 v2.13 纳入回归，后续继续依据实际问题扩展。

候选方向会随代码、真实验证和剩余时间调整；未实现项不计为完成。原生安装、升级、恢复等既有人工验收记录仍以 `refactor-acceptance.json` 为准。
