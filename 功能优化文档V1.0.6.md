1. 进程管理

    可以根据命令关键字/进程名称进行查询，可以通过端口进行查询

    需要优化查询速度
2. 图片处理

    - 格式转换：目标格式应该包含JPG、PNG、GIF、SVG
    - 批量缩放：尺寸调整应该直接包含照片常见尺寸，比如1寸，2寸等
    - 批量水印：应该增加预览、水印位置应该增加按间距平铺、水印应该增加旋转角度调整
    - 裁剪工具：应该能够预览图片，能够在预览图上通过可调整方框的方式，自动填入裁剪坐标和比例完成裁剪。
    - 方向调整：应该增加预览、操作中增加自定义功能：可以自定义旋转角度、自定义翻转角度。
    - 图片转PDF：页面尺寸应该增加更多常见页面尺寸。
3. 文本工具

    - json工具：对于任何报错信息，均修改报错为：不是JSON格式数据。
4. 视频处理

    - 格式转换：增加质量预设为原画。
    - 视频压缩：修改FFMPEG PRESET为通用描述，方便用户理解。
    - 视频截取：增加视频播放预览，播放原始视频，原始视频可以拖动左右两边调节时间条，完成开始时间和结束时间的选取。
    - 音频导出：增加视频播放预览，播放原始视频，原始视频可以拖动左右两边调节时间条，完成开始时间和结束时间的选取。
5. 文件管理

    - 文件搜索：更改为开源文件搜索引擎之类的，以优化性能。
    - 压缩/解压：压缩为ZIP时，没有提供密码选项，需要提供密码选项。
6. EXCEL工具

    - 暂不处理。
7. PDF工具

    - PDF转高清图片：支持提供默认：超清、高清、标清选项，修改DPI，也支持自定义DPI。目标格式应该包含JPG、PNG、GIF、SVG。并且增加一行DPI小字说明，告诉用户DPI与清晰度的关系，推荐超清、高清、标清的DPI数值。
    - PDF合并：要支持对每个添加进入合并列表的PDF进行单独的页码选择，支持填入页码和勾选页码，勾选页码时支持预览界面来勾选页码。然后再将选中的PDF的选中的页码，进行合并。
    - 拆分PDF：除了按固定页数进行拆分PDF，还应该支持按页码进行拆分PDF，支持填入多个页码区间或多个指定页码，一次性拆分成多个PDF。实际上就是将页码切割功能和拆分PDF合并，并且支持一次性拆分生成多个PDF。
    - 页面重排：去除页码顺序填写框，支持预览PDF，支持预览页面进行拖动调整，自动填入新页码顺序。
    - 图片转PDF：实际上与图片处理工具中的图片转PDF是相同功能，这里仅仅引用页面过来展示，或提示用户点击前往图片处理工具的图片转PDF页面。直接引用过来展示更好。
    - PDF转WORD：新增功能，将PDF转换为带格式的可编辑的WORD。
    - OCR识别：后期功能。
8. 公章生成

    - 解决导出时的报错：生成失败:PDF. resolve output path() missing 2 required positional arguments: 'output path' and 'sufix
    - 将公章生成功能添加密码，提示敏感功能需要输入密码才可以访问该功能，密码为Jd_251114
9. 修改软件图标，将任何使用LOGO的地方，都换成logo.png图片，该文件已经放在项目根目录下，把它复制任何需要它的地方。