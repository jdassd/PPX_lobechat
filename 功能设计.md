# 多功能工具箱功能设计

## 1. 产品定位与设计目标
- **定位**：面向桌面端用户的轻量级多工具集，强调“开箱即用”“界面优美”“常用功能聚合”。
- **设计目标**：
  1. 以卡片化信息密度呈现常用工具，维持统一视觉语言（柔和渐变+玻璃拟态阴影）。
  2. 工具配置与状态在前端可视化管理，Python 侧提供 API 服务，确保模块化扩展。
  3. 10 秒内完成核心操作（如截图标注、单位换算），降低来回切换 App 的成本。

## 2. 用户画像 & 场景
| 用户 | 需求痛点 | 工具箱价值 |
| --- | --- | --- |
| 产品经理/设计师 | 频繁截图、颜色采集、快速原型记录 | 集成截图、取色、Markdown 便签，减少工具切换 |
| 开发者 | 需要正则/JSON 格式化、接口调试、小型脚本 | 提供格式化、Mock API、快速脚本运行器 |
| 运营/客服 | 批量文本处理、翻译、快捷回复 | 文本批处理、翻译、短语库，提高响应效率 |

## 3. 信息架构
- **顶栏**：品牌 + 全局搜索 + 云同步状态。
- **左侧导航**：按功能组折叠（效率、创意、系统、工具集市），支持拖拽排序。
- **主工作区**：卡片栅格（3 列）；每张卡片可进入「扩展视图」弹窗。
- **右侧辅助栏**：历史记录 / 收藏夹 / 当前工具设置。

## 4. 功能模块明细
### 4.1 效率工具
1. **智能便签**：Markdown/富文本双模式，支持待办勾选、双向链接；数据存 pyapp TinyDB。
2. **多格式剪贴板历史**：捕获文本/图片/链接，按标签筛选，一键粘贴。
3. **计划番茄钟**：倒计时 + 白噪音；统计推送到系统通知。

### 4.2 创意工具
1. **截图标注 & 取色**：桌面截图、矩形/笔刷标注、导出 PNG/SVG，内置取色器复制 HEX/RGB。
2. **灵感白板**：轻量画布，贴纸/图片/手写，支持导出 PDF。
3. **素材压缩器**：图像压缩、GIF 转 MP4，调用 Python Pillow/ffmpeg。

### 4.3 系统工具
1. **文件整理助手**：基于规则（类型/日期）批量移动到指定目录，含执行前预览。
2. **网络诊断**：Ping/Traceroute 可视化，异常时生成报告。
3. **系统资源监控**：CPU/GPU/内存实时曲线，异常阈值提醒。

### 4.4 工具集市（扩展）
- **文本批处理**：大小写转换、去重、正则替换、JSON/YAML/SQL 格式化。
- **单位与汇率换算**：重量/长度/货币实时接口（可缓存最后一次汇率）。
- **翻译与短语库**：调用第三方 API（可插拔），常用语片段收藏。
- **快速脚本执行**：Python/Node 模板，安全沙箱，输出日志。

## 5. 交互与视觉要点
- **主题**：浅色默认，深色可切换；半透明面板 + 柔焦背景；Element Plus 自定义主题 Token。
- **动效**：卡片 hover 提升层级，工具切换 200ms 动画；进度/操作结果使用 toast + 右上角通知。
- **无障碍**：键盘导航、ARIA 标签、色弱模式（高对比度调色板）。

## 6. 技术实现建议
- **前端 (gui/)**：Vue 3 + Pinia；每个工具为独立组件，统一通过 `ToolRegistry` 动态加载。Vite 动态导入减少主包体积。
- **后端 (pyapp/)**：`api/toolkit.py` 提供统一入口；I/O 密集工具使用 `asyncio`；任务长时间运行时通过事件流推送状态。
- **数据同步**：本地 `TinyDB` 存储自定义配置，支持导入/导出；后续可加入云端同步钩子。
- **插件机制**：定义 `manifest.json`（name、icon、entry、permissions），在 GUI 中热加载。

## 7. 体验闭环
1. 用户通过搜索快速定位工具 → 打开卡片。
2. 操作用时 ≤10 秒，结果可复制/导出。
3. 重要操作有历史记录，便于回溯。
4. 所有工具支持收藏与桌面小组件化（粘性卡片）。

## 8. 迭代路线
- **MVP**：剪贴板历史、文本批处理、单位换算、系统资源监控。
- **版本 1.1**：截图取色、文件整理助手、翻译短语库。
- **版本 1.2**：灵感白板、插件集市、云同步。

