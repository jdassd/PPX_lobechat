# 更新日志

## v2.4.0 - 2026-08-02

### 任务洞察与批量恢复

- 任务中心新增工具/状态筛选、分页、批量选择、批量取消和批量重试。
- 新增任务成功率、平均耗时、活动任务与异常任务统计；后端列表 API 支持状态、方法、关键词和分页过滤。
- 修复 Excel 列画像已在前端进入任务队列、但任务与工作流后端白名单缺失而被拒绝的问题。

### 更可靠的自动化

- 工作流步骤新增最多 5 次失败重试与退避等待，并在运行记录中保存每次尝试的状态和耗时。
- 周期任务和目录监听支持即时启停与手动运行；运行记录可按状态、来源和关键词筛选。
- 修复工作流结束时运行记录对象被原地清空、返回结果与历史步骤为空的问题。

### 文档索引与文本诊断

- 文档中心支持单文件与目录混合增量索引，并检测源文件变化、缺失和搜索结果过期状态。
- 搜索摘要新增安全的关键词高亮，不使用原始 HTML 注入。
- 文本工具新增按行/按词 Diff，支持忽略空白与大小写、变化统计和 Unified Diff。
- 新增 JWT 本地诊断，展示 Header、Payload、时间声明和风险提示；明确不验证签名且不写入任务历史。

### 工程质量

- 新增 v2.4 专项回归测试，覆盖任务筛选/统计/批量操作、工作流重试与触发器、索引健康状态、JWT 和文本 Diff。
- 保持任务单 worker、安全方法白名单和本地优先边界，不新增大型运行时依赖。

## v2.3.0 - 2026-08-02

### 持久任务与自动化

- 任务中心升级为 Python 持久队列，支持暂停、取消、失败重试、重启中断恢复、进度与结果导出。
- 新增安全白名单工作流，支持步骤参数引用、失败策略、运行记录和内置模板。
- 新增周期触发器与目录监听；文件稳定后提交到同一任务队列，已有文件不会被重复触发。
- 首页和命令面板新增收藏、最近使用与收藏优先排序。

### 文档、PDF 与数据

- 新增本地文档中心，可增量索引 PDF、DOCX、XLSX、Markdown、CSV、JSON 与文本文件并全文搜索。
- 新增表格 OCR，可从图片或 PDF 重建规则表格并导出 XLSX、CSV、JSON。
- 新增 PDF 页面工作台，支持缩略图预览、页面重排、删除、旋转和页码。
- 新增 PDF 安全副本，支持可见水印、永久文字遮盖、元数据清理和 AES-256 密码保护。
- Excel 新增列画像、按列拆分与可导出的数据质量报告。
- 图片工具补齐旋转翻转、拼接和批量命名界面。

### 可恢复文件操作与桌面体验

- 安全删除按批次写入清单，可在 PPX 回收站查看、恢复或按保留天数清理。
- 批量重命名记录事务清单，可一键撤销且永不覆盖已有文件。
- 新增跨平台健康检查、完整本地备份、校验后延迟恢复和隐私安全诊断报告。
- 恢复前自动创建安全备份；备份路径经过穿越校验，界面偏好只接受 `ppx-` 前缀。
- 记忆窗口尺寸与位置，支持通过命令行文件参数打开对应工具，PDF 可直接载入页面工作台。
- 自动更新除可信 HTTPS 域名和文件大小外，新增 GitHub Release Asset SHA-256 摘要强制校验。

### 工程质量

- 新增 v2.3 专项回归测试，覆盖任务队列、工作流绑定、文件恢复、PDF、安全脱敏、表格 OCR、全文索引和备份恢复。
- 所有新增能力继续使用统一 API 返回格式、任务白名单和本地存储边界。

## v2.0.0 - 2026-08-02

### 工作流与界面

- 首页改为任务导向的工作台，常用动作可直接进入具体子功能。
- 新增全局功能搜索，支持搜索“扫描 PDF OCR”“合并 Excel”等具体动作。
- 新增任务中心，本地记录处理状态、耗时、结果路径与失败原因。
- 新增模块中心，核心办公模块常驻，视频、印章、思维导图和系统诊断可按需启用。
- 新增本机能力检测，集中展示 OCR、FFmpeg、LibreOffice、Playwright 和平台能力状态。

### 新能力

- 新增离线图片 OCR，识别结果可预览并导出 UTF-8 文本。
- 新增扫描 PDF OCR，可导出文本或带隐形文字层的可搜索 PDF。
- PyInstaller 打包链路新增 RapidOCR 模型与 ONNX Runtime 动态库收集。

### 安全与破坏性变更

- 移除“软件管理 / 强力粉碎”界面和公共 API。
- 移除自定义启动命令的新增、删除与执行；启动项改为只读诊断。
- 批量删除强制先预览，只能移动到源目录下的 `.ppx_recycle`，不再支持永久删除。
- 批量重命名强制先预览，发生名称冲突时跳过，不再覆盖已有文件。
- ZIP / 7Z 解压新增成员路径边界校验，拒绝路径穿越归档。
- 系统进程页改为只读诊断，移除任意 PID 强制结束接口。

### 工程质量

- 增加 v2 能力、OCR 和安全策略单元测试。
- 发布构建增加前端静态检查与 Python 单元测试门禁。
