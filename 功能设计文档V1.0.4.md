# PPX 多功能工具箱 - 版本V1.0.4功能设计文档

**文档版本**: 1.0
**设计日期**: 2025-11-13
**目标版本**: V1.0.4
**当前版本**: V1.0.3

---

## 第一部分：版本概述

### 设计理念
基于V1.0.3已有的6大功能模块（PDF处理、Excel处理、公章生成、进程管理、系统工具、数据存储），V1.0.4应**聚焦提高用户生产效率和数据处理的便利性**，新增功能遵循以下原则：
- **高频需求**：选择日常工作中最常见的操作
- **工具互补**：与现有功能形成完整的效率工具链
- **易用性**：降低操作门槛，提供智能化辅助
- **无侵入性**：新功能独立开发，不修改既有模块

---

## 第二部分：新增功能模块（4个）

### 功能1：图片处理工具（ImageTool）

#### 1.1 功能定义
跨格式图片处理集成工具，覆盖日常办公中的图片操作需求。

#### 1.2 具体功能

| 功能名 | 功能描述 | 输入 | 输出 | 优先级 |
|-------|--------|------|------|--------|
| **图片格式转换** | 支持JPEG、PNG、BMP、WEBP、TIFF相互转换 | 源格式图片 | 目标格式图片 | P0 |
| **批量缩放** | 按像素/百分比缩放图片尺寸，支持保持比例 | 图片+缩放参数 | 缩放后图片 | P0 |
| **批量添加水印** | 支持图片、文字水印，可调位置、透明度、尺寸 | 图片+水印素材 | 添加水印后的图片 | P1 |
| **图片裁剪** | 支持自定义区域裁剪、按比例裁剪 | 图片+裁剪参数 | 裁剪后图片 | P1 |
| **批量压缩** | 支持质量压缩(30%-95%)和尺寸压缩 | 图片+压缩模式 | 压缩后图片 | P0 |
| **图片拼接** | 支持横向/纵向拼接多张图片 | 多张图片+拼接方向 | 拼接后大图 | P2 |
| **旋转/翻转** | 支持旋转90/180/270°、镜像/翻转 | 图片+操作类型 | 变换后图片 | P1 |
| **批量重命名** | 按模式(序号/时间戳/自定义)重命名 | 图片文件夹 | 重命名后的文件 | P2 |
| **EXIF数据查看** | 查看图片的拍摄信息、地理位置、设备信息 | 图片 | EXIF元数据表格 | P2 |

#### 1.3 前端设计

```
ImageTool 组件 (gui/src/components/image/ImageTool.vue)
├─ Tab页签结构：
│  ├─ 格式转换 Tab
│  │  ├─ 文件选择区
│  │  ├─ 目标格式下拉选择
│  │  ├─ 质量滑块(JPEG)
│  │  └─ 批量转换按钮
│  │
│  ├─ 缩放工具 Tab
│  │  ├─ 图片选择（支持拖拽）
│  │  ├─ 缩放模式选择(像素/百分比)
│  │  ├─ 宽度/高度输入
│  │  ├─ 保持比例复选框
│  │  └─ 处理按钮
│  │
│  ├─ 水印添加 Tab
│  │  ├─ 图片选择
│  │  ├─ 水印类型切换(文字/图片)
│  │  ├─ 文字水印：文本/字号/颜色/透明度/字体
│  │  ├─ 图片水印：上传图片/尺寸/透明度
│  │  ├─ 位置选择器(九宫格)
│  │  ├─ 实时预览
│  │  └─ 导出按钮
│  │
│  ├─ 裁剪工具 Tab
│  │  ├─ 图片选择与预览
│  │  ├─ 裁剪模式切换(自由/固定比例)
│  │  ├─ 拖拽框选或参数输入
│  │  ├─ 裁剪参数(x, y, w, h)
│  │  └─ 确认按钮
│  │
│  ├─ 压缩工具 Tab
│  │  ├─ 文件选择
│  │  ├─ 压缩模式(质量/尺寸/均衡)
│  │  ├─ 质量滑块(P0) 或 目标大小输入(P1)
│  │  ├─ 原始/压缩后大小对比
│  │  └─ 批量压缩
│  │
│  ├─ 旋转/翻转 Tab
│  │  ├─ 图片选择
│  │  ├─ 操作按钮(旋转90°/180°/270°、镜像、翻转)
│  │  ├─ 实时预览
│  │  └─ 保存
│  │
│  ├─ 图片转PDF Tab (与PdfTool互补)
│  │  ├─ 拖拽排序区(支持添加/删除)
│  │  ├─ 页面尺寸选择(A4/A5/自定义)
│  │  ├─ 排列模式(1张/2张/4张 per page)
│  │  └─ 转换按钮
│  │
│  └─ 其他高级功能 (P2/折叠区)
│     ├─ 拼接、重命名、EXIF查看
```

#### 1.4 后端设计 (api/image.py)

```python
class Image:
    def image_format_convert(options):
        """格式转换"""
        # input: source_file, target_format, quality(for JPEG)
        # output: converted_file_path

    def image_batch_resize(options):
        """批量缩放"""
        # input: file_list, width, height, keep_ratio
        # output: resized_file_paths

    def image_add_watermark(options):
        """添加水印"""
        # input: source_file, watermark_type(text/image), params
        # output: watermarked_file_path

    def image_crop(options):
        """裁剪"""
        # input: source_file, x, y, width, height
        # output: cropped_file_path

    def image_batch_compress(options):
        """批量压缩"""
        # input: file_list, mode(quality/size), level
        # output: compressed_file_paths, size_reduce %

    def image_rotate_flip(options):
        """旋转/翻转"""
        # input: source_file, operation(rotate90/180/270/mirror/flip)
        # output: rotated_file_path

    def image_to_pdf(options):
        """图片转PDF"""
        # input: image_list, page_size, arrangement
        # output: pdf_file_path

    # P2优先级函数
    def image_concat(options):  # 拼接
    def image_batch_rename(options):  # 重命名
    def image_get_exif(options):  # 获取EXIF信息
```

**技术栈选型**: Pillow(PIL) / opencv-python

---

### 功能2：文本与数据处理工具（TextTool）

#### 2.1 功能定义
高频文本处理工具集，覆盖文本变换、数据格式转换、编解码等需求。

#### 2.2 具体功能

| 功能名 | 功能描述 | 输入 | 输出 | 优先级 |
|-------|--------|------|------|--------|
| **文本编码/解码** | Base64、URL、HTML实体编解码，UTF-8/GBK转换 | 文本 | 编码/解码后文本 | P0 |
| **正则表达式匹配** | 支持搜索、替换、提取匹配的文本 | 文本+正则表达式 | 匹配结果 | P1 |
| **JSON格式化** | JSON美化、压缩、验证、路径查询 | JSON字符串 | 格式化JSON/查询结果 | P0 |
| **CSV与JSON相互转换** | CSV转JSON、JSON转CSV、自定义分隔符 | CSV/JSON文件 | 转换后文件 | P1 |
| **文本大小写转换** | 支持大写、小写、句首大写、标题大写、驼峰互转 | 文本 | 变换后文本 | P0 |
| **文本去重与排序** | 去重、按字母序/长度/自定义排序、统计词频 | 多行文本 | 处理后文本 | P1 |
| **时间戳转换** | 时间戳与日期时间互转，支持多时区 | 时间戳或日期 | 转换结果 | P1 |
| **MD5/SHA256哈希** | 计算字符串或文件的哈希值 | 文本或文件 | 哈希值 | P0 |
| **批量字符替换** | 支持多条规则同时替换，可导入导出规则 | 文本+规则列表 | 替换后文本 | P2 |
| **Unicode转义处理** | Unicode编码点与字符相互转换 | 文本 | 转换结果 | P2 |

#### 2.3 前端设计

```
TextTool 组件 (gui/src/components/text/TextTool.vue)
├─ Tab页签结构：
│  ├─ 编码/解码 Tab
│  │  ├─ 编码类型选择(Base64/URL/HTML/UTF-8/GBK)
│  │  ├─ 左/右两列对比编辑框
│  │  ├─ 编码/解码按钮
│  │  └─ 复制按钮
│  │
│  ├─ 正则表达式 Tab
│  │  ├─ 文本输入区
│  │  ├─ 正则表达式输入
│  │  ├─ 操作选择(查找/替换/提取)
│  │  ├─ 搜索结果或替换文本
│  │  ├─ 匹配项高亮显示
│  │  └─ 导出结果
│  │
│  ├─ JSON工具 Tab
│  │  ├─ JSON输入区
│  │  ├─ 功能按钮(美化/压缩/验证/查询)
│  │  ├─ 格式化结果展示
│  │  ├─ 路径查询输入框($.path.to.field)
│  │  └─ 复制按钮
│  │
│  ├─ CSV/JSON转换 Tab
│  │  ├─ 文件选择与预览
│  │  ├─ 分隔符选择/自定义
│  │  ├─ 转换方向选择(CSV→JSON/JSON→CSV)
│  │  └─ 转换按钮
│  │
│  ├─ 文本转换 Tab
│  │  ├─ 文本输入区
│  │  ├─ 转换选项(大写/小写/标题/驼峰等)
│  │  ├─ 快速按钮组
│  │  ├─ 实时预览
│  │  └─ 复制按钮
│  │
│  ├─ 去重排序 Tab
│  │  ├─ 多行文本输入
│  │  ├─ 操作选择(去重/排序/词频统计)
│  │  ├─ 排序方式选择
│  │  ├─ 处理结果
│  │  └─ 统计信息面板
│  │
│  ├─ 时间戳转换 Tab
│  │  ├─ 时间戳输入(支持秒/毫秒)
│  │  ├─ 日期选择器
│  │  ├─ 时区选择
│  │  ├─ 双向转换
│  │  └─ 实时转换
│  │
│  ├─ 哈希计算 Tab
│  │  ├─ 文本/文件选择
│  │  ├─ 哈希算法选择(MD5/SHA256)
│  │  ├─ 计算按钮
│  │  ├─ 哈希结果显示
│  │  └─ 复制按钮
│  │
│  └─ 高级功能 (P2/折叠区)
│     ├─ 批量替换、Unicode转义
```

#### 2.4 后端设计 (api/text.py)

```python
class Text:
    def text_encode_decode(options):
        """编码/解码"""
        # input: content, codec_type, operation
        # output: encoded/decoded_content

    def text_regex_match(options):
        """正则表达式操作"""
        # input: content, pattern, operation(search/replace/extract)
        # output: match_results or replaced_content

    def text_format_json(options):
        """JSON格式化"""
        # input: json_string, operation(format/compress/validate/query)
        # output: formatted_json or query_result

    def text_convert_csv_json(options):
        """CSV与JSON转换"""
        # input: file_path, direction, delimiter
        # output: converted_file_path

    def text_case_transform(options):
        """大小写转换"""
        # input: content, case_type
        # output: transformed_content

    def text_deduplicate_sort(options):
        """去重与排序"""
        # input: content, operation, sort_method
        # output: processed_content, stats

    def text_timestamp_convert(options):
        """时间戳转换"""
        # input: timestamp_or_date, timezone, operation
        # output: converted_datetime

    def text_hash_calculate(options):
        """哈希计算"""
        # input: content_or_file, hash_type
        # output: hash_value

    # P2优先级函数
    def text_batch_replace(options):  # 批量替换
    def text_unicode_convert(options):  # Unicode转义处理
```

**技术栈选型**: hashlib, json, re, base64, urllib.parse, datetime

---

### 功能3：视频处理工具（VideoTool）

#### 3.1 功能定义
轻量级视频处理工具，针对日常视频编辑和格式转换需求。

#### 3.2 具体功能

| 功能名 | 功能描述 | 输入 | 输出 | 优先级 |
|-------|--------|------|------|--------|
| **视频格式转换** | MP4、AVI、MOV、MKV、WebM等格式相互转换 | 源视频 | 目标格式视频 | P0 |
| **视频压缩** | 按码率/尺寸/预设压缩，支持预览编码时间 | 视频+压缩参数 | 压缩后视频 | P0 |
| **视频截取** | 按时间范围截取视频片段 | 视频+起止时间 | 截取后视频 | P0 |
| **视频提取音频** | 提取视频中的音频为MP3/WAV/AAC | 视频 | 音频文件 | P1 |
| **批量提取帧图** | 提取视频每N帧或每N秒一张图片 | 视频+间隔参数 | 图片序列 | P1 |
| **视频合成** | 多个视频文件拼接为一个 | 视频列表 | 合成视频 | P2 |
| **水印添加** | 添加图片或文字水印到视频 | 视频+水印 | 带水印视频 | P2 |

#### 3.3 前端设计

```
VideoTool 组件 (gui/src/components/video/VideoTool.vue)
├─ Tab页签结构：
│  ├─ 格式转换 Tab
│  │  ├─ 文件选择
│  │  ├─ 目标格式下拉
│  │  ├─ 预设质量(高/中/低)
│  │  ├─ 处理进度条
│  │  └─ 转换按钮
│  │
│  ├─ 视频压缩 Tab
│  │  ├─ 文件选择与信息显示
│  │  ├─ 压缩模式(码率/尺寸/预设)
│  │  ├─ 参数滑块(码率/分辨率)
│  │  ├─ 预计文件大小显示
│  │  ├─ 预计编码时间显示
│  │  └─ 压缩按钮
│  │
│  ├─ 视频截取 Tab
│  │  ├─ 文件选择
│  │  ├─ 时间轴拖拽(起点/终点)
│  │  ├─ 或时间输入框(hh:mm:ss)
│  │  ├─ 时长显示
│  │  └─ 截取按钮
│  │
│  ├─ 提取音频 Tab
│  │  ├─ 视频选择
│  │  ├─ 音频格式选择(MP3/WAV/AAC)
│  │  ├─ 音质选择
│  │  └─ 提取按钮
│  │
│  ├─ 提取帧图 Tab
│  │  ├─ 视频选择
│  │  ├─ 提取模式(每N帧/每N秒)
│  │  ├─ 间隔参数输入
│  │  ├─ 输出目录选择
│  │  └─ 提取按钮
│  │
│  ├─ 视频信息 Tab
│  │  ├─ 视频选择
│  │  ├─ 信息显示表(时长/分辨率/码率/帧率/编码)
│  │  └─ 刷新按钮
│  │
│  └─ 高级功能 (P2/折叠区)
│     ├─ 视频合成、字幕嵌入、水印、GIF制作
```

#### 3.4 后端设计 (api/video.py)

```python
class Video:
    def video_format_convert(options):
        """格式转换"""
        # input: source_file, target_format, quality_preset
        # output: converted_file_path, progress_callback

    def video_compress(options):
        """视频压缩"""
        # input: source_file, mode(bitrate/size/preset), level
        # output: compressed_file_path, estimated_time

    def video_cut(options):
        """视频截取"""
        # input: source_file, start_time, end_time
        # output: cut_file_path

    def video_extract_audio(options):
        """提取音频"""
        # input: source_file, audio_format, quality
        # output: audio_file_path

    def video_extract_frames(options):
        """提取帧图"""
        # input: source_file, mode(frame_interval/time_interval), interval
        # output: frame_image_paths, total_count

    def video_get_info(options):
        """获取视频信息"""
        # input: source_file
        # output: {duration, resolution, bitrate, codec, fps, ...}

    # P2优先级函数
    def video_concat(options):  # 合成
    def video_add_subtitle(options):  # 添加字幕
    def video_add_watermark(options):  # 添加水印
    def video_to_gif(options):  # 转为GIF
```

**技术栈选型**: ffmpeg-python / moviepy

**说明**: 此功能依赖FFmpeg库，需在`pnpm run init`时检测/安装FFmpeg。

---

### 功能4：文件管理工具（FileTool）

#### 4.1 功能定义
增强型文件管理与批处理工具，提高日常文件操作效率。

#### 4.2 具体功能

| 功能名 | 功能描述 | 输入 | 输出 | 优先级 |
|-------|--------|------|------|--------|
| **文件搜索** | 支持按名称、大小、修改时间、文件类型搜索 | 目录+搜索条件 | 符合条件的文件列表 | P0 |
| **文件批量复制** | 支持按名称/类型筛选，一键复制到目标目录 | 源目录+过滤条件 | 复制结果统计 | P1 |
| **文件批量删除** | 支持按过滤条件安全删除，可撤销 | 源目录+过滤条件 | 删除结果统计 | P1 |
| **文件去重** | 按文件内容(哈希)或名称查找并清理重复文件 | 目录 | 重复文件清单+节省空间 | P1 |
| **文件批量改名** | 支持序号/正则/替换/日期戳等多种重命名规则 | 文件列表+规则 | 重命名结果 | P1 |
| **目录结构分析** | 生成树形目录结构、统计文件数/大小分布 | 目录 | 目录树/统计信息 | P0 |
| **文件分类整理** | 按文件类型/大小/日期自动分类到子目录 | 目录+分类规则 | 分类结果 | P2 |
| **快速压缩包** | 支持ZIP/7Z/RAR等，支持密码保护 | 文件/文件夹 | 压缩包文件 | P1 |
| **快速解压** | 支持多种压缩格式解压，预览内容 | 压缩包 | 解压文件 | P1 |
| **文件对比** | 对比两个文件的差异(文本/二进制) | 两个文件 | 差异高亮显示 | P2 |

#### 4.3 前端设计

```
FileTool 组件 (gui/src/components/file/FileTool.vue)
├─ 公共导航区：
│  ├─ 面包屑导航(支持快速跳转)
│  └─ 刷新按钮
│
├─ Tab页签结构：
│  ├─ 文件搜索 Tab
│  │  ├─ 目录选择
│  │  ├─ 搜索条件面板
│  │  │  ├─ 文件名模糊搜索
│  │  │  ├─ 文件类型多选
│  │  │  ├─ 文件大小范围
│  │  │  ├─ 修改时间范围
│  │  │  └─ 递归搜索复选框
│  │  ├─ 搜索结果表格(可排序)
│  │  └─ 搜索按钮
│  │
│  ├─ 批量复制 Tab
│  │  ├─ 源目录选择
│  │  ├─ 过滤条件(文件类型/名称模式)
│  │  ├─ 文件预览列表
│  │  ├─ 目标目录选择
│  │  ├─ 操作选项(覆盖/跳过)
│  │  └─ 复制按钮
│  │
│  ├─ 批量删除 Tab
│  │  ├─ 源目录选择
│  │  ├─ 过滤条件
│  │  ├─ 待删除文件列表(含确认提示)
│  │  ├─ 操作选项(永久删除/回收站)
│  │  └─ 删除按钮
│  │
│  ├─ 文件去重 Tab
│  │  ├─ 目录选择
│  │  ├─ 去重模式(内容哈希/名称)
│  │  ├─ 重复文件分组显示
│  │  ├─ 保留策略选择(最大/最小/最新)
│  │  ├─ 节省空间计算显示
│  │  └─ 清理按钮
│  │
│  ├─ 批量改名 Tab
│  │  ├─ 文件夹选择
│  │  ├─ 重命名规则选择(序号/日期/正则/替换)
│  │  ├─ 规则参数输入
│  │  ├─ 文件预览(原名→新名对比)
│  │  ├─ 预览按钮
│  │  └─ 执行改名按钮
│  │
│  ├─ 目录结构分析 Tab
│  │  ├─ 目录选择
│  │  ├─ 树形结构显示(支持折叠/展开)
│  │  ├─ 统计面板
│  │  │  ├─ 总文件数/总大小
│  │  │  ├─ 类型分布(饼图)
│  │  │  └─ 大小分布(柱图)
│  │  ├─ 导出按钮(CSV/JSON)
│  │  └─ 刷新按钮
│  │
│  ├─ 压缩与解压 Tab
│  │  ├─ 选项卡: 压缩 / 解压
│  │  ├─ 压缩模式:
│  │  │  ├─ 文件/文件夹选择
│  │  │  ├─ 压缩格式选择(ZIP/7Z)
│  │  │  ├─ 压缩级别(滑块)
│  │  │  ├─ 密码输入(可选)
│  │  │  └─ 压缩按钮
│  │  └─ 解压模式:
│  │     ├─ 压缩包选择
│  │     ├─ 内容预览
│  │     ├─ 解压目录选择
│  │     ├─ 密码输入(if needed)
│  │     └─ 解压按钮
│  │
│  └─ 高级功能 (P2/折叠区)
│     ├─ 文件分类整理、文件对比
```

#### 4.4 后端设计 (api/file.py)

```python
class File:
    def file_search(options):
        """文件搜索"""
        # input: root_dir, name_pattern, file_types, size_range, date_range, recursive
        # output: file_list with metadata

    def file_batch_copy(options):
        """批量复制"""
        # input: source_dir, filters, target_dir, conflict_policy
        # output: copy_count, copy_result

    def file_batch_delete(options):
        """批量删除"""
        # input: source_dir, filters, delete_policy
        # output: delete_count, delete_result

    def file_deduplicate(options):
        """文件去重"""
        # input: root_dir, dedupe_mode(content/name)
        # output: duplicate_groups, space_saved

    def file_batch_rename(options):
        """批量改名"""
        # input: file_list, rename_rule, rule_params
        # output: rename_result, preview

    def file_directory_analyze(options):
        """目录结构分析"""
        # input: root_dir
        # output: tree_structure, statistics

    def file_compress(options):
        """文件压缩"""
        # input: source_path, target_format, compression_level, password
        # output: compressed_file_path

    def file_decompress(options):
        """文件解压"""
        # input: archive_file, target_dir, password
        # output: extract_result, file_list

    # P2优先级函数
    def file_auto_classify(options):  # 自动分类
    def file_compare(options):  # 文件对比
```

**技术栈选型**: zipfile / py7zr / pathlib / shutil

---

## 第三部分：现有功能增强

### 增强1：PDF工具增强
- **新增功能**：
  - PDF页面重新排序（拖拽排序界面）
  - PDF提取文本（支持指定页码范围）
  - PDF提取图片（提取所有或指定页的图片）
  - 图片转PDF（将单张/多张图片转换为PDF）

### 增强2：进程管理增强
- **新增功能**：
  - 进程性能监控（CPU使用率、内存趋势图）
  - 进程启动时间排序
  - 批量任务管理（同时操作多个进程）
  - 进程自动启动规则保存
  - 禁用/启用指定进程联网

### 增强3：UI/UX改进
- **全局改进**：
  - 深色/浅色主题切换
  - 快捷键支持（Ctrl+C复制结果等）

---

## 第四部分：开发优先级规划

### Phase 1 (MVP阶段)
**P0级功能**（核心功能）：
- 图片处理：格式转换、缩放、压缩
- 文本处理：编码解码、JSON工具、文本转换、哈希
- 文件管理：文件搜索、目录分析、压缩解压
- 视频处理：格式转换、压缩、截取

**预期成果**：完成4个工具的P0级功能，发布V1.0.4-beta

### Phase 2 (功能完善)
**P1级功能**：
- 图片水印、裁剪、旋转翻转
- 文本正则、CSV/JSON、去重排序、时间戳、批量替换
- 文件批量操作、去重、改名
- 视频提取音频、提取帧

**预期成果**：完成P1级功能，发布V1.0.4-rc

### Phase 3 (高级特性)
**P2级功能**（高级特性）：
- 图片拼接、重命名
- 文本Unicode处理
- 文件分类、对比
- 视频合成

**预期成果**：完成P2级功能，发布V1.0.4正式版

### Phase 4 (现有功能增强)
- PDF增强
- Excel增强
- 进程管理增强
- UI/UX全局改进

---

## 第五部分：技术架构设计

### 后端架构改进
```
api/
├─ __init__.py (API总入口，继承所有功能类)
├─ api.py (主API类)
├─ image.py (NEW - 图片处理)
├─ text.py (NEW - 文本处理)
├─ video.py (NEW - 视频处理)
├─ file.py (NEW - 文件管理)
├─ pdf.py (现有，计划增强)
├─ excel.py (现有，计划增强)
├─ system.py (现有，计划增强)
├─ seal.py (现有)
├─ storage.py (现有)
└─ utils/ (NEW - 通用工具函数)
   ├─ validators.py (参数验证)
   ├─ converters.py (格式转换)
   ├─ progress.py (进度回调)
   └─ error_handler.py (错误处理)
```

### 前端架构改进
```
gui/src/components/
├─ image/ (NEW)
│  └─ ImageTool.vue
├─ text/ (NEW)
│  └─ TextTool.vue
├─ video/ (NEW)
│  └─ VideoTool.vue
├─ file/ (NEW)
│  └─ FileTool.vue
├─ pdf/
│  └─ PdfTool.vue (现有，计划增强)
├─ excel/
│  └─ ExcelTool.vue (现有，计划增强)
├─ seal/
│  └─ SealTool.vue (现有)
├─ system/
│  └─ ProcessManager.vue (现有，计划增强)
├─ shared/ (NEW - 共享组件)
│  ├─ FileSelector.vue (文件选择器)
│  ├─ ProgressBar.vue (进度条)
│  ├─ ResultTable.vue (结果表格)
│  └─ PreviewPanel.vue (预览面板)
└─ BtnUpdate.vue (现有)
```

### 关键库依赖添加

| 库名 | 功能 | 备注 |
|-----|-----|-----|
| Pillow | 图片处理 | pip install Pillow |
| ffmpeg-python | 视频处理 | 需要系统FFmpeg |
| opencv-python | 高级图像处理 |  |
| zipfile、py7zr | 压缩解压 | 标准库+额外库 |
| python-magic | 文件类型识别 |  |

---

## 第六部分：数据库设计

### 新增存储表（可选，用于记录操作历史）

**表1：operation_history**
```
{
  id: 唯一ID,
  operation_type: "image_convert" | "text_encode" | "video_compress" | "file_search",
  operation_name: 用户可读的操作名称,
  input_params: {原始输入参数},
  output_result: {输出结果摘要},
  status: "success" | "failed",
  error_message: 错误信息(如有),
  create_time: 时间戳,
  duration: 耗时(毫秒)
}
```

**表2：user_settings**
```
{
  key: 设置键,
  value: 设置值,
  updated_time: 修改时间
}
例：
- last_used_module: 上次使用的功能模块
- default_output_dir: 默认输出目录
- theme_mode: "light" | "dark"
- recent_files: 最近使用文件列表
```

---

## 第七部分：用户交互优化

### 快捷键规划
| 快捷键 | 功能 |
|--------|------|
| Ctrl+N | 新建/清空 |
| Ctrl+O | 打开文件 |
| Ctrl+S | 保存结果 |
| Ctrl+C | 复制结果 |
| Ctrl+V | 粘贴 |
| Ctrl+Z | 撤销 |
| Tab | 功能模块切换 |
| F1 | 帮助/文档 |

### 反馈机制
- **操作成功**：绿色提示气泡 + 声音提示(可关闭)
- **操作失败**：红色错误提示 + 错误日志
- **长时间操作**：显示进度条 + 取消按钮
- **文件拖拽**：支持从文件管理器直接拖拽文件到工具

---

## 第八部分：测试策略

### 单元测试
```
tests/
├─ test_image.py
├─ test_text.py
├─ test_video.py
├─ test_file.py
└─ test_utils.py
```

### 集成测试
- 跨工具链测试（如图片→PDF、视频→GIF）
- 前后端通信测试
- 大文件处理测试

### 性能测试
- 批量处理性能基准（1000+文件）
- 内存占用监控
- 长时间运行稳定性测试

---

## 第九部分：文档与支持

### 用户文档
- 每个功能模块配对使用指南
- 常见问题FAQ
- 快捷键帮助面板（F1呼出）
- 在线教程链接

### 开发者文档
- API接口规范文档
- 前后端通信协议
- 贡献指南和代码风格

---

## 第十部分：版本发布计划

### V1.0.4-beta
- Phase 1完成
- 4个新工具的P0级功能
- 基础UI完成

### V1.0.4-rc
- Phase 1 + Phase 2完成
- P0 + P1级功能完成
- 性能优化和bug修复

### V1.0.4 正式版
- Phase 1 + Phase 2 + Phase 3完成
- 现有功能部分增强
- 完整文档和测试覆盖

---

## 附录：功能对标与市场分析

| 功能类别 | PPX V1.0.4 | 竞品对标 | 差异化 |
|--------|-----------|--------|--------|
| 图片处理 | 10+功能 | 类似ACDSee、Photoshop | 轻量级、批量、无订阅 |
| 文本处理 | 10+功能 | 类似在线工具集 | 离线、隐私、快速 |
| 视频处理 | 8+功能 | 类似剪映、快剪辑 | 轻量级、无广告、专业级 |
| 文件管理 | 10+功能 | 类似Total Commander | 现代UI、深度集成 |

**市场定位**：轻量级、全能、跨平台、隐私优先的本地工具箱，对标Everything(文件搜索) + XnConvert(图片) + MediaInfo(视频) + Beyond Compare(对比)的综合方案。

---

**文档完成日期**: 2025-11-13
**下一步行动**: 待批准后启动Phase 1开发

